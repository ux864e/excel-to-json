//! Shared test utilities for excel-to-json integration tests.

/// Common module for shared helpers. Currently serves as the
/// marker that prevents this directory from being compiled as
/// an independent test target.
